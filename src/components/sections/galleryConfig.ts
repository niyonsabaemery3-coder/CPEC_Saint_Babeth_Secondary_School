// One page/slide = up to this many photos. Shared between Gallery.tsx (the
// bento grid on the page) and Lightbox.tsx (the fullscreen viewer), so both
// always agree on how photos are grouped and paged.
export const GALLERY_PAGE_SIZE = 5;
