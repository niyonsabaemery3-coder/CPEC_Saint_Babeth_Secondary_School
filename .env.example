# Frontend environment — copy to .env and adjust for your setup.
# Vite only exposes variables prefixed with VITE_ to the client bundle.

# URL of the backend API. During local dev this is the Express server.
# In production (GitHub Pages), set this as a repo variable in the Actions
# workflow or here before building.
VITE_API_URL=http://localhost:4000
